// Cypress support file
// Place here any common commands and hooks for all tests

// Example:
// Cypress.Commands.add('login', (email, password) => { ... })

beforeEach(() => {
  // Any setup before each test can go here
});
